# 🚀 快速修复指南

## ✅ 已完成
- 添加群组过滤功能到代码
- 创建配置文档
- 提交到本地 Git

## 🔧 现在需要做的（3 个步骤）

### 步骤 1: 查找群组 JID（2分钟）

访问 Supabase: https://supabase.com/dashboard

在 SQL Editor 运行:
```sql
SELECT jid, name FROM whatsapp_contacts 
WHERE jid LIKE '%@g.us' 
ORDER BY updated_at DESC;
```

找到 "Casey 与 Casey 的对话群组"，复制它的 `jid`（例如: `120363123456789@g.us`）

### 步骤 2: 配置白名单（1分钟）

编辑 `server.js` 第 26-30 行:

**修改前:**
```javascript
const ALLOWED_WEBHOOK_GROUPS = [
    // 'XXXXXXXXXX@g.us',  // Casey 与 Casey 的对话群组
];
```

**修改后:**
```javascript
const ALLOWED_WEBHOOK_GROUPS = [
    '120363123456789@g.us',  // Casey 与 Casey 的对话群组（替换为实际JID）
];
```

### 步骤 3: 部署（3分钟）

```bash
# 1. 提交配置
git add server.js
git commit -m "配置允许的群组白名单"
git push origin main

# 2. SSH 到服务器
ssh ubuntu@whatsapp-crm.techforliving.app -i ~/.ssh/claw2.pem

# 3. 更新代码
cd /home/ubuntu/whatsapp-bot
git pull origin main

# 4. 重启服务
pm2 restart whatsapp-crm

# 5. 查看日志（确认生效）
pm2 logs whatsapp-crm
```

## 📝 测试

1. **在 "Casey 与 Casey 的对话群组" 发消息**
   - 期望: 机器人回复 ✅
   - 日志: `✅ 允许的群组消息，触发 webhook`

2. **在 "和Lamlam的群组" 发消息**
   - 期望: 机器人不回复 ⛔
   - 日志: `⛔ 群组消息被过滤，不触发 webhook`

## ❓ 需要帮助？

详细文档:
- `FIXING_SUMMARY.md` - 完整修复说明
- `GROUP_FILTER_CONFIG.md` - 配置指南
- `如何查找群组JID.md` - JID 查找方法

## 🎯 完成后的效果

- ✅ 机器人只在 "Casey 与 Casey 的对话群组" 回复
- ⛔ 机器人不在其他群组回复（包括 "和Lamlam的群组"）
- 📝 所有消息仍然保存和显示在前端

---
总时间: 约 6 分钟
